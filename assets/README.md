# Background image (optional)

Place a document-themed photo here as `document-bg.jpg` for the bottom gradient background.

If the file is missing, the app uses an online fallback image automatically.

Recommended: wide landscape photo (1920×1080 or larger) of papers, books, or a desk workspace.
