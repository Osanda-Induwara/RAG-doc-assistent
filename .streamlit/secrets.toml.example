# Optional — copy to secrets.toml only if you prefer this over .env
# Local path: .streamlit/secrets.toml (this folder)
# Or user-wide: C:\Users\YOUR_NAME\.streamlit\secrets.toml
#
# For Streamlit Community Cloud: add the same key in the app's Secrets UI.

OPENAI_API_KEY = "sk-your-key-here"
